namespace Reversi.Core;

public enum OpponentKind
{
    /// <summary>Two players sharing the same device.</summary>
    Human = 1,

    /// <summary>One player against the engine.</summary>
    Computer = 2,
}
